#include "WTools/log.hpp"

// WtMessage::WtMessage(const char *message, WtMessageType type, bool visible, bool logged) : message(message), type(type), visible(visible) {
//     if (type == WtMessageType::LOG && this->visible) {
//         this->visible = false;
//     }
// }

// void Wtlog(const char *message, WtMessageType type, bool visible, bool log) {
// }